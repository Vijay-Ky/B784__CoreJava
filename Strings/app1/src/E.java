public class E {

	public static void main(String[] args) {
		String s1 = "hello";
		int i = s1.length();
		System.out.println(i);
		System.out.println(s1.length());
	}
}
/*
 * in case of Arrays length is the property but in case of string it is a method.
 * in case of Arrays length is used for finding out the size of the array.
 * in case of Strings length is used for finding out the number of characters available inside the string object.
 * in case of Arrays length should not end with parenthesis but in case of strings it should.
 * 
 * */

